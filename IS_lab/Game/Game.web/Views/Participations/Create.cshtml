@model Game.Domain.DomainModels.Participation

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Participation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <!--TODO: Put the action name where the form should be submitted-->
        <form asp-action="Create">
            <input type="hidden" asp-for="AthleteId" value="@Model.AthleteId" class="form-control" />
            <div class="form-group">
                <label asp-for="CompetitionId" class="control-label"></label>
                <select asp-for="CompetitionId" class="form-control" asp-items="ViewBag.CompetitionId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
}
